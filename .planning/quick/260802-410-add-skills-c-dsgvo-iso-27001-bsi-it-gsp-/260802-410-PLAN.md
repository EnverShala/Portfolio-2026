---
phase: quick-260802-410
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Portfolio.dc.html
  - dist/index.html
autonomous: false
requirements: [CNT-QUICK-410]

must_haves:
  truths:
    - "Skills grid shows C#, DSGVO, ISO 27001 and BSI IT GSP tiles in both DE and EN"
    - "Continually learning stays the last skill tile"
    - "EN hero rotator cycles: FRONTEND DEVELOPER, FULLSTACK DEVELOPER, AI & AUTOMATION, PROBLEM SOLVER"
    - "DE hero rotator cycles fully in German: FRONTEND ENTWICKLER, FULLSTACK ENTWICKLER, KI & AUTOMATISIERUNG, PROBLEMLÖSER"
    - "dist/index.html stays byte-identical to Portfolio.dc.html (deploy artifact in sync)"
  artifacts:
    - path: "Portfolio.dc.html"
      provides: "Source single-file React/DCLogic app: CONTENT i18n object + SKILLS array"
      contains: "SKILLS = ["
    - path: "dist/index.html"
      provides: "Deploy copy of the source file"
      contains: "SKILLS = ["
  key_links:
    - from: "CONTENT.de.hero.roles / CONTENT.en.hero.roles"
      to: "roleRef typewriter loop"
      via: "this.CONTENT[this.state.lang].hero.roles (Portfolio.dc.html:696)"
      pattern: "hero\\.roles"
    - from: "SKILLS array"
      to: "<sc-for list=\"{{ skills }}\">"
      via: "skills: this.SKILLS (Portfolio.dc.html:497)"
      pattern: "skills: this\\.SKILLS"
---

<objective>
Add four skills (C#, DSGVO, ISO 27001, BSI IT GSP) to the Skills section and localize the hero role rotator: EN gets "AI & Automation" instead of "AI Developer", DE gets fully German role words.

Purpose: Reflect security/compliance qualifications and fix the DE hero that currently shows English role titles.
Output: Updated `Portfolio.dc.html` and its byte-identical deploy copy `dist/index.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Source of truth: `D:\Vizionists\portfolio2026\Portfolio.dc.html` — a single-file app
(custom `DCLogic` component template syntax + React UMD, no bundler).

**Build model — VERIFIED, read this before editing:**
- There is NO build script (no `package.json`, no `Makefile`, no `*.ps1`/`*.sh`, no `scripts/`).
- `dist/index.html` is currently **byte-identical** to `Portfolio.dc.html` (`diff` returns empty).
- The established workflow (see commit `b97a1c3`, which added Python + Docker) is:
  **apply the exact same edit to both files** in the same commit.
- Therefore both files share identical line numbers — the line references below are valid for both.

**i18n model — VERIFIED:** one JS object `CONTENT = { de: {...}, en: {...} }` inside the
class (`Portfolio.dc.html:324-419`). Language toggled by `toggleLang()` (line 441) which
also resets the typewriter state. There are NO separate language HTML files and no
translation JSON.

**Skills model — VERIFIED:** `SKILLS` is a single language-agnostic array
(`Portfolio.dc.html:421-439`) of `{ mark, name }` objects, passed to the template as
`skills` (line 497) and rendered by `<sc-for list="{{ skills }}" as="sk">` (line 219).
Skill names are NOT translated — one edit location per file covers both languages.

<interfaces>
Skill entry shape (Portfolio.dc.html:421-439):
  { mark: string, name: string }
  `mark` renders as the large green glyph, `name` as the small mono caption.
  Existing marks are 1-3 chars or a symbol: '</>', '#', 'JS', 'TS', 'Py', 'Dk', '∞'.

Hero roles shape (Portfolio.dc.html:329 / 376):
  roles: string[]   // typewriter cycles through these
  Rendered inside a span with `text-transform: uppercase` (line 158),
  so source strings stay Title Case and display as ALL CAPS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add C#, DSGVO, ISO 27001, BSI IT GSP to the SKILLS array</name>
  <files>Portfolio.dc.html, dist/index.html</files>
  <action>
In `Portfolio.dc.html`, in the `SKILLS` array, insert four new entries directly AFTER
`{ mark: 'Dk', name: 'Docker' },` (line 437) and BEFORE
`{ mark: '∞', name: 'Continually learning' }` (line 438). "Continually learning" must
remain the last entry — it is the closing tile of the grid.

New entries, matching the existing two-space indentation and `{ mark: 'X', name: 'Y' },` formatting:
  { mark: 'C#', name: 'C#' },
  { mark: '§', name: 'DSGVO' },
  { mark: 'ISO', name: 'ISO 27001' },
  { mark: 'BSI', name: 'BSI IT GSP' },

Mark rationale: marks are short glyphs, not full names — `C#` is already short enough to
be its own mark, `§` reads as legal/compliance for DSGVO, `ISO`/`BSI` are the standard
short forms. `BSI IT GSP` is the requested abbreviation of "BSI IT-Grundschutz-Praktiker";
use it verbatim as the `name` — do NOT expand it (the caption is 10.5px and the tile is a
1:1 square, a long name wraps badly).

Append rather than reordering by category: commit `b97a1c3` appended Python and Docker the
same way, so this preserves the established grid order.

Do NOT translate the names per language — `SKILLS` is shared by both DE and EN, and
`DSGVO` is the term the user asked for in both.

Do NOT change `hint-placeholder-count="14"` on the `<sc-for>` (line 219). It is a
design-time placeholder hint, was already stale at 14 with 17 skills, and commit `b97a1c3`
deliberately left it untouched.

Then apply the byte-identical edit to `dist/index.html` at the same location, since
`dist/index.html` is a plain copy of the source with no build step.

File is UTF-8 and already contains umlauts and glyphs (`Problemlöser`, `∞`, `◭`) — preserve
encoding; do not let `§` or `∞` become mojibake.
  </action>
  <verify>
    <automated>cd "D:/Vizionists/portfolio2026" && for f in Portfolio.dc.html dist/index.html; do for s in "name: 'C#'" "name: 'DSGVO'" "name: 'ISO 27001'" "name: 'BSI IT GSP'"; do grep -qF "$s" "$f" || { echo "MISSING $s in $f"; exit 1; }; done; done; grep -n "Continually learning" Portfolio.dc.html; grep -c "mark:" Portfolio.dc.html; diff dist/index.html Portfolio.dc.html && echo "IN SYNC"</automated>
  </verify>
  <done>Both files contain all four new skill entries, `mark:` count is 21, "Continually learning" is still the final array entry, and `diff dist/index.html Portfolio.dc.html` returns empty.</done>
</task>

<task type="auto">
  <name>Task 2: Localize the hero role rotator (EN "AI & Automation", DE fully German)</name>
  <files>Portfolio.dc.html, dist/index.html</files>
  <action>
Two single-line edits per file, in the `CONTENT` object.

1. German (`Portfolio.dc.html:329`, inside `CONTENT.de.hero`) — replace
   `roles: ['Frontend Developer', 'Fullstack Developer', 'AI Developer', 'Problemlöser'],`
   with
   `roles: ['Frontend Entwickler', 'Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser'],`

2. English (`Portfolio.dc.html:376`, inside `CONTENT.en.hero`) — replace
   `roles: ['Frontend Developer', 'Fullstack Developer', 'AI Developer', 'Problem Solver'],`
   with
   `roles: ['Frontend Developer', 'Fullstack Developer', 'AI & Automation', 'Problem Solver'],`

Keep the strings Title Case, NOT ALL CAPS. The rendering span at line 158 applies
`text-transform: uppercase`, so these display as FRONTEND ENTWICKLER / KI & AUTOMATISIERUNG /
AI & AUTOMATION. Hardcoding uppercase in the array would be redundant and would break the
casing convention used by every other CONTENT string.

Keep the array length at 4 and keep `Problemlöser` / `Problem Solver` in last position —
the typewriter indexes with `roles[this.roleI % roles.length]` (line 697) and needs no other
change.

Touch ONLY the two `roles:` lines. Leave `about.intro`, `contact.need` and every other
"AI & Fullstack Developer" prose string exactly as-is — this task is scoped to the hero
rotator only.

Then apply the byte-identical edits to `dist/index.html` at the same two lines.
  </action>
  <verify>
    <automated>cd "D:/Vizionists/portfolio2026" && for f in Portfolio.dc.html dist/index.html; do grep -qF "roles: ['Frontend Entwickler', 'Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser']," "$f" || { echo "DE roles wrong in $f"; exit 1; }; grep -qF "roles: ['Frontend Developer', 'Fullstack Developer', 'AI & Automation', 'Problem Solver']," "$f" || { echo "EN roles wrong in $f"; exit 1; }; grep -qF "'AI Developer'" "$f" && { echo "stale AI Developer in $f"; exit 1; }; done; diff dist/index.html Portfolio.dc.html && echo "IN SYNC"; node --input-type=module -e "1" 2>/dev/null; echo OK</automated>
  </verify>
  <done>Both files have the new DE and EN `roles` arrays, no `'AI Developer'` string remains anywhere, other CONTENT prose is unchanged, and `diff dist/index.html Portfolio.dc.html` returns empty.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Four new skill tiles (C#, DSGVO, ISO 27001, BSI IT GSP) added before "Continually learning", and the hero typewriter localized — EN now rotates "AI & AUTOMATION", DE rotates fully German role titles.</what-built>
  <how-to-verify>
1. Serve the deploy copy from the repo root, e.g. `npx serve dist` or open `dist/index.html` directly in a browser (no build step is needed — it is a plain single-file app).
2. Hero, default language: watch the typewriter cycle through all four roles. Confirm no English word appears while the site is in German — expect FRONTEND ENTWICKLER → FULLSTACK ENTWICKLER → KI & AUTOMATISIERUNG → PROBLEMLÖSER.
3. Click the language toggle to EN and watch the rotator again — expect FRONTEND DEVELOPER → FULLSTACK DEVELOPER → AI & AUTOMATION → PROBLEM SOLVER. The toggle resets the animation, so wait for a full cycle.
4. Confirm the `&` renders as an ampersand (not `&amp;`) in both languages.
5. Scroll to the Skills section. Confirm four new tiles appear after Docker: `C#` / C#, `§` / DSGVO, `ISO` / ISO 27001, `BSI` / BSI IT GSP — and that "Continually learning" (∞) is still the very last tile.
6. Confirm the tile captions are not clipped or overflowing the square tiles — "ISO 27001" and "BSI IT GSP" are the longest captions in the grid; check at desktop width and at a narrow mobile width (the grid is `auto-fill, minmax(110px, 1fr)`).
7. Confirm `§` and `∞` render correctly (no `?` or mojibake) — this catches an encoding regression.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what looks wrong (e.g. caption clipping, wrong mark glyph, animation not cycling).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Changes are static display strings in an existing file; no new input, network call, or dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-410-01 | Tampering | `dist/index.html` drift from `Portfolio.dc.html` | mitigate | Every task's `<verify>` runs `diff dist/index.html Portfolio.dc.html` and requires empty output |
| T-410-02 | Information Disclosure | New skill/role strings | accept | Public portfolio copy the user explicitly authored; no PII beyond what is already published |
| T-410-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — no build step exists in this repo |
</threat_model>

<verification>
```bash
cd "D:/Vizionists/portfolio2026"
# 1. Deploy copy in sync with source (must print nothing)
diff dist/index.html Portfolio.dc.html && echo "IN SYNC"
# 2. Skill count went 17 -> 21
grep -c "mark:" Portfolio.dc.html   # expect 21
# 3. No stale hero role
grep -c "AI Developer" Portfolio.dc.html   # expect 0
# 4. Only the intended lines changed
git diff --stat
```
`git diff` should show exactly two files with a small, symmetric changeset
(4 skill lines + 2 role lines per file).
</verification>

<success_criteria>
- `Portfolio.dc.html` and `dist/index.html` both contain the four new SKILLS entries, with "Continually learning" still last.
- `grep -c "mark:" Portfolio.dc.html` returns 21.
- DE hero roles are `['Frontend Entwickler', 'Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser']`.
- EN hero roles are `['Frontend Developer', 'Fullstack Developer', 'AI & Automation', 'Problem Solver']`.
- No `'AI Developer'` string remains in either file.
- `diff dist/index.html Portfolio.dc.html` returns empty.
- Human checkpoint approved (tiles render unclipped, rotator cycles correctly in both languages, glyphs not mojibake).
</success_criteria>

<output>
Create `.planning/quick/260802-410-add-skills-c-dsgvo-iso-27001-bsi-it-gsp-/260802-410-SUMMARY.md` when done.
</output>
