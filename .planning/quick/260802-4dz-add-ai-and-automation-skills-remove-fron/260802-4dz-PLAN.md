---
phase: quick-260802-4dz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Portfolio.dc.html
  - dist/index.html
autonomous: false
requirements: [QUICK-260802-4dz]

must_haves:
  truths:
    - "Skills grid shows an 'AI' tile and an 'Automation' tile, both before 'Continually learning'"
    - "Hero typewriter never shows 'Frontend Developer' (EN) or 'Frontend Entwickler' (DE)"
    - "About-me intro describes the author as a Software Developer / Software-Entwickler in both languages"
    - "Skills section intro text no longer contains the word frontend in either language"
    - "Contact section reads 'Need a Problem solver? Contact me!' (EN) and 'Brauchst du einen Problemlöser? Kontaktiere mich!' (DE)"
    - "Portfolio.dc.html and dist/index.html remain byte-identical"
  artifacts:
    - path: "Portfolio.dc.html"
      provides: "Source of truth for SKILLS array and CONTENT.de/CONTENT.en i18n dictionaries"
      contains: "name: 'Automation'"
    - path: "dist/index.html"
      provides: "Deployed copy, must mirror Portfolio.dc.html exactly"
      contains: "name: 'Automation'"
  key_links:
    - from: "SKILLS array (Portfolio.dc.html ~line 421-445)"
      to: "Skills grid render"
      via: "existing SKILLS.map render — no wiring change needed"
      pattern: "mark: 'AI'"
    - from: "CONTENT.de.contact.need / CONTENT.en.contact.need"
      to: "line 261 template"
      via: "{{ t.contact.need }} <strong>{{ t.contact.needCta }}</strong>"
      pattern: "Problem ?[Ss]olver|Problemlöser"
---

<objective>
Five content-only text changes to the portfolio: add AI + Automation skill tiles, drop the "Frontend Developer" hero role, retitle the about-me intro to Software Developer, remove "frontend" from the skills blurb, and rewrite the contact call-to-action — all in both DE and EN, in both `Portfolio.dc.html` and `dist/index.html`.

Purpose: The user positions himself as an AI/Fullstack generalist; "Frontend" is redundant next to "Fullstack" and narrows the perceived scope. The contact CTA should lead with problem-solving rather than a job title.
Output: Two edited HTML files (source + deploy copy), byte-identical, committed together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260802-410-add-skills-c-dsgvo-iso-27001-bsi-it-gsp-/260802-410-SUMMARY.md

@Portfolio.dc.html
@dist/index.html

<project_facts>
- Static site, no build step. `dist/index.html` is a **byte-identical copy** of `Portfolio.dc.html` (verified: `diff dist/index.html Portfolio.dc.html` is currently empty). Every edit must be applied to BOTH files, identically. Line numbers below are valid for both files.
- React + Babel standalone, rendered in-browser. All user-facing copy lives in the `CONTENT = { de: {...}, en: {...} }` object (lines 324-419) and the `SKILLS = [...]` array (lines 421-443).
- Files are UTF-8; glyph marks like `§`, `∞`, `◆`, `⎇` and umlauts (`ö` in Problemlöser) already render correctly. Do not let an editor rewrite the encoding.
- Established pattern (prior task 260802-410, commit d2dda70): the German dictionary is **fully German** — no English job titles leak into `CONTENT.de`.
- No other file in the repo contains these strings (`grep -ril "fullstack\|frontend"` returns only these two files).
</project_facts>

<interfaces>
<!-- Exact current state of every line to be touched. Same line numbers in both files. -->

SKILLS array tail (Portfolio.dc.html:436-443):
  line 436: `    { mark: 'Py', name: 'Python' },`
  line 437: `    { mark: 'Dk', name: 'Docker' },`
  line 438: `    { mark: 'C#', name: 'C#' },`
  line 439: `    { mark: '§', name: 'DSGVO' },`
  line 440: `    { mark: 'ISO', name: 'ISO 27001' },`
  line 441: `    { mark: 'BSI', name: 'BSI IT GSP' },`
  line 442: `    { mark: '∞', name: 'Continually learning' }`   <-- no trailing comma, must stay last
  line 443: `  ];`

CONTENT.de (line 329, 335, 342, 364) and CONTENT.en (line 376, 382, 389, 411) — see each task for verbatim before/after.

Contact render template (line 261, unchanged):
`<p data-reveal style="...">{{ t.contact.need }} <strong style="color: var(--green);">{{ t.contact.needCta }}</strong></p>`
=> `need` is the question, `needCta` is the green bold call-to-action. The user's requested sentences split exactly across this pair.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI + Automation skill tiles and drop the Frontend hero role</name>
  <files>Portfolio.dc.html, dist/index.html</files>
  <action>
Apply both edits below to `Portfolio.dc.html` AND `dist/index.html` identically (same strings, same positions).

**1a — SKILLS array.** Insert two new entries between `{ mark: 'BSI', name: 'BSI IT GSP' },` (line 441) and `{ mark: '∞', name: 'Continually learning' }` (line 442), keeping the existing 4-space indentation:

    { mark: 'AI', name: 'AI' },
    { mark: '⚙', name: 'Automation' },

Mark choice is Claude's discretion, exercised here: `AI` follows the letter-abbreviation style already used by `Py` / `Dk` / `ISO` / `BSI`; `⚙` (U+2699 GEAR) follows the glyph style of `◭` / `◈` / `⎇` / `✦` / `↻` / `◆` / `∞`. Do not use an emoji-presentation gear (no U+FE0F variation selector) — the plain glyph matches how the sibling marks render. `{ mark: '∞', name: 'Continually learning' }` must remain the final element and must keep having no trailing comma; the new lines DO need trailing commas.

**1b — Hero role rotator.** Remove only the Frontend entry from both role arrays; keep the remaining three in order.

Line 329 (DE) before:
`        roles: ['Frontend Entwickler', 'Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser'],`
Line 329 (DE) after:
`        roles: ['Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser'],`

Line 376 (EN) before:
`        roles: ['Frontend Developer', 'Fullstack Developer', 'AI & Automation', 'Problem Solver'],`
Line 376 (EN) after:
`        roles: ['Fullstack Developer', 'AI & Automation', 'Problem Solver'],`

Do not touch the rotator/typewriter logic — it derives its cycle length from the array, so a 3-element array needs no other change. Preserve `ö` in `Problemlöser` and the literal `&` (not `&amp;`) in `KI & Automatisierung`.

Commit both files together: `feat(skills): add AI and Automation tiles, drop Frontend hero role`
  </action>
  <verify>
    <automated>cd "D:/Vizionists/portfolio2026" && diff dist/index.html Portfolio.dc.html && [ "$(grep -c "mark:" Portfolio.dc.html)" = "23" ] && grep -q "name: 'Automation'" Portfolio.dc.html && grep -q "name: 'AI' }" Portfolio.dc.html && [ "$(grep -c "Frontend Entwickler\|Frontend Developer" Portfolio.dc.html)" = "0" ] && grep -q "roles: \['Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser'\]," Portfolio.dc.html && grep -q "roles: \['Fullstack Developer', 'AI & Automation', 'Problem Solver'\]," Portfolio.dc.html && echo TASK1_PASS</automated>
  </verify>
  <done>Both files byte-identical; `mark:` count is 23 (was 21); AI and Automation entries sit between BSI IT GSP and Continually learning; zero occurrences of "Frontend Entwickler"/"Frontend Developer"; both roles arrays have exactly 3 elements in the specified order.</done>
</task>

<task type="auto">
  <name>Task 2: Retitle about-me intro, de-frontend the skills blurb, rewrite the contact CTA</name>
  <files>Portfolio.dc.html, dist/index.html</files>
  <action>
Apply all six string replacements below to `Portfolio.dc.html` AND `dist/index.html` identically. Replace only the quoted substring shown; leave the rest of each line (key name, quoting style, trailing comma) untouched. Note line 382 uses double quotes because of the apostrophe in "I'm" — keep double quotes there.

**2a — About-me intro (item 3: title becomes Software Developer).**

Line 335 (DE) — replace `deutschsprachiger AI- & Fullstack-Entwickler` with `deutschsprachiger Software-Entwickler`. Resulting line:
`        intro: 'Hi, ich bin ein deutschsprachiger Software-Entwickler aus Stuttgart. Angetrieben von den grenzenlosen Möglichkeiten in der IT, gestalte ich mit Begeisterung visuell fesselnde und intuitive Websites und Anwendungen.',`

Line 382 (EN) — replace `AI & Fullstack Developer` with `Software Developer`. Resulting line:
`        intro: "Hi, I'm a german speaking Software Developer based in Stuttgart. Motivated by the limitless opportunities within IT, I am excited about crafting visually captivating and intuitive websites and applications.",`

German wording is a planner decision: `Software-Entwickler` (hyphenated, mirroring the `Fullstack-Entwickler` form it replaces) rather than the literal English "Software Developer", because the DE dictionary was deliberately made fully German in prior task 260802-410 (commit d2dda70). This is surfaced for confirmation in Task 3 — if the user prefers the literal English term in DE, it is a one-word swap on line 335.

**2b — Skills section blurb (item 4: remove the word "frontend").**

Line 342 (DE) before: `        p1: 'Ich habe Erfahrung darin gesammelt, Projekte mit verschiedenen Frontend-Technologien und Konzepten zu bauen.',`
Line 342 (DE) after:  `        p1: 'Ich habe Erfahrung darin gesammelt, Projekte mit verschiedenen Technologien und Konzepten zu bauen.',`

Line 389 (EN) before: `        p1: 'I have gained experience in building projects with different frontend technologies and concepts.',`
Line 389 (EN) after:  `        p1: 'I have gained experience in building projects with different technologies and concepts.',`

The word is embedded as the compound `Frontend-Technologien` (DE) and as the adjective `frontend technologies` (EN); deleting the token plus its hyphen/space leaves both sentences grammatical with no further rewording needed.

**2c — Contact call-to-action (item 5).** The requested sentence spans the `need` + `needCta` pair rendered at line 261 (`{{ t.contact.need }} <strong>{{ t.contact.needCta }}</strong>`). Only `need` changes; `needCta` already matches the requested wording in both languages — leave `needCta` exactly as is.

Line 364 (DE) before: `        need: 'Brauchst du einen AI- oder Fullstack-Entwickler?', needCta: 'Kontaktiere mich!',`
Line 364 (DE) after:  `        need: 'Brauchst du einen Problemlöser?', needCta: 'Kontaktiere mich!',`

Line 411 (EN) before: `        need: 'Need an AI or Fullstack developer?', needCta: 'Contact me!',`
Line 411 (EN) after:  `        need: 'Need a Problem solver?', needCta: 'Contact me!',`

Keep the EN capitalisation `Problem solver` exactly as the user specified (capital P, lowercase s). Do not touch `contact.q` on lines 362/409 ("Ein Problem zu lösen?" / "Got a problem to solve?") — that is the section sub-heading, a separate string.

After these edits, `grep -i frontend` must return zero matches across both files, and `Fullstack` must survive only in the two hero role arrays.

Commit both files together: `feat(content): retitle intro to Software Developer, broaden skills copy, new contact CTA`
  </action>
  <verify>
    <automated>cd "D:/Vizionists/portfolio2026" && diff dist/index.html Portfolio.dc.html && [ "$(grep -ci "frontend" Portfolio.dc.html)" = "0" ] && [ "$(grep -c "Fullstack" Portfolio.dc.html)" = "2" ] && grep -q "deutschsprachiger Software-Entwickler aus Stuttgart" Portfolio.dc.html && grep -q "a german speaking Software Developer based in Stuttgart" Portfolio.dc.html && grep -q "Projekte mit verschiedenen Technologien und Konzepten" Portfolio.dc.html && grep -q "projects with different technologies and concepts" Portfolio.dc.html && grep -q "need: 'Brauchst du einen Problemlöser?', needCta: 'Kontaktiere mich!'," Portfolio.dc.html && grep -q "need: 'Need a Problem solver?', needCta: 'Contact me!'," Portfolio.dc.html && echo TASK2_PASS</automated>
  </verify>
  <done>Both files byte-identical; zero case-insensitive matches for "frontend"; exactly 2 matches for "Fullstack" (the two hero role arrays only); all six replacement strings present verbatim in both files; `contact.q` and both `needCta` values unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all five content changes</name>
  <files>(none - verification only)</files>
  <action>Pause and hand off to the user. Present the what-built summary and the numbered verification steps below, then wait for the resume signal. Do not self-approve. If the user requests the German title wording change, apply it to both files and re-run the Task 2 verify command.</action>
  <what-built>
All five requested content changes applied symmetrically to `Portfolio.dc.html` and `dist/index.html`: (1) AI + Automation skill tiles, (2) Frontend removed from both hero rotators, (3) about-me intro now Software Developer / Software-Entwickler, (4) "frontend" removed from both skills blurbs, (5) contact CTA rewritten. Automated checks confirmed string presence, absence of stale strings, and file sync.
  </what-built>
  <how-to-verify>
1. Open `dist/index.html` in a browser (double-click, or `npx serve dist`).
2. Hero, DE: the typewriter should cycle exactly FULLSTACK ENTWICKLER → KI & AUTOMATISIERUNG → PROBLEMLÖSER. No "Frontend" should ever appear. Toggle to EN: FULLSTACK DEVELOPER → AI & AUTOMATION → PROBLEM SOLVER. Confirm `&` renders as an ampersand, not `&amp;`.
3. About me: DE should read "Hi, ich bin ein deutschsprachiger Software-Entwickler aus Stuttgart…", EN "Hi, I'm a german speaking Software Developer based in Stuttgart…". **Decision to confirm:** the German version uses the German term "Software-Entwickler". If you want the literal English "Software Developer" in the German version too, say so — it is a one-word change.
4. Skills section intro: DE "…Projekte mit verschiedenen Technologien und Konzepten zu bauen." / EN "…projects with different technologies and concepts." — no mention of frontend, and the sentence should read naturally.
5. Skills grid: two new tiles, AI (mark "AI") and Automation (gear glyph ⚙), appear after BSI IT GSP and before the ∞ "Continually learning" tile. Confirm the gear glyph renders as a gear and not as a box/question mark, and that the tile captions are not clipped at both desktop and narrow mobile widths.
6. Contact section: DE "Brauchst du einen Problemlöser? **Kontaktiere mich!**" / EN "Need a Problem solver? **Contact me!**" — the second half green and bold. The sub-heading above it ("Ein Problem zu lösen?" / "Got a problem to solve?") is unchanged by design; confirm the two lines together don't read repetitively.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what to adjust (e.g. German title wording, gear glyph choice)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → deployed static site | Edited HTML ships directly to production with no build/validation step |
| browser → in-page JS strings | All edited strings are React text children, not `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4dz-01 | Tampering | `SKILLS` array / `CONTENT` object literals | mitigate | Edits are inside single-quoted JS string literals; no quotes, backslashes or `</script>` sequences introduced. Task verify greps assert the exact resulting literals parse-identical to the specified strings. |
| T-4dz-02 | Denial of Service | Broken JS syntax breaks the whole page (Babel standalone fails silently) | mitigate | Trailing-comma rule stated explicitly for the new SKILLS entries; Task 3 browser check catches a blank/unrendered page immediately. |
| T-4dz-03 | Information disclosure | `dist/index.html` drifting from source | mitigate | `diff dist/index.html Portfolio.dc.html` gate in every task's `<verify>`. |
| T-4dz-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no dependency surface introduced. |
| T-4dz-04 | Information disclosure | Injected content is public marketing copy | accept | No secrets, PII, or credentials involved. |
</threat_model>

<verification>
```bash
cd "D:/Vizionists/portfolio2026"

# 1. Source and deploy copy identical
diff dist/index.html Portfolio.dc.html && echo "IN SYNC"

# 2. Skill tiles: 21 -> 23, Continually learning still last
grep -c "mark:" Portfolio.dc.html            # expect 23
grep -n "mark:" Portfolio.dc.html | tail -3  # expect AI, Automation, Continually learning in that order

# 3. Frontend fully gone, Fullstack survives only in the hero rotators
grep -ci "frontend" Portfolio.dc.html        # expect 0
grep -ci "frontend" dist/index.html          # expect 0
grep -n "Fullstack" Portfolio.dc.html        # expect exactly lines 329 and 376 (roles arrays)

# 4. New copy present in both files
for f in Portfolio.dc.html dist/index.html; do
  grep -q "deutschsprachiger Software-Entwickler" "$f" &&
  grep -q "a german speaking Software Developer" "$f" &&
  grep -q "Projekte mit verschiedenen Technologien" "$f" &&
  grep -q "projects with different technologies and concepts" "$f" &&
  grep -q "Brauchst du einen Problemlöser?" "$f" &&
  grep -q "Need a Problem solver?" "$f" && echo "$f OK"
done

# 5. Encoding intact (umlauts + glyphs)
grep -c "Problemlöser" Portfolio.dc.html     # expect 2 (hero role + contact need)
grep -c "∞" Portfolio.dc.html                # expect 1

# 6. Changeset is exactly the two HTML files
git diff --stat HEAD~2..HEAD -- . | tail -3
```
</verification>

<success_criteria>
- [ ] `diff dist/index.html Portfolio.dc.html` returns empty
- [ ] `grep -c "mark:"` returns 23 in both files; AI and Automation are the two entries immediately before `Continually learning`, which remains last and comma-free
- [ ] `grep -ci "frontend"` returns 0 in both files
- [ ] `grep -c "Fullstack"` returns 2 in both files (hero role arrays only)
- [ ] DE roles array is `['Fullstack Entwickler', 'KI & Automatisierung', 'Problemlöser']`; EN is `['Fullstack Developer', 'AI & Automation', 'Problem Solver']`
- [ ] About intro reads "Software-Entwickler" (DE) / "Software Developer" (EN)
- [ ] Skills `p1` reads naturally in both languages with no frontend reference
- [ ] `contact.need` is `'Brauchst du einen Problemlöser?'` (DE) / `'Need a Problem solver?'` (EN); `needCta` untouched
- [ ] `contact.q` (lines 362/409) unchanged
- [ ] Umlauts and glyphs render correctly — no mojibake, no `?` boxes
- [ ] Two commits, each touching only `Portfolio.dc.html` and `dist/index.html`
- [ ] Human verification checkpoint approved (including the DE "Software-Entwickler" wording decision)
</success_criteria>

<output>
Create `.planning/quick/260802-4dz-add-ai-and-automation-skills-remove-fron/260802-4dz-SUMMARY.md` when done
</output>
