---
phase: quick-260803-344
plan: 01
subsystem: ui
tags: [dcloop, svg, mobile-nav, pointerdown, icons]

# Dependency graph
requires: []
provides:
  - "Document-level pointerdown listener that closes the mobile burger menu on any tap outside #main-nav"
  - "Inline SVG replacing the faint dotted-circle glyph on the middle About-me bullet"
  - "Dedicated icon:'gear' skills-template branch rendering an inline SVG cog for the Automation tile"
affects: [ui, mobile-nav, skills-section, about-section]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "setupXxx()/this._onXxx lifecycle convention extended to a new setupOutsideClick() method"
    - "SVG icon branches (sk.icon == '<key>') for skill tiles needing crisp, colour-controllable glyphs instead of emoji/text marks"

key-files:
  created: []
  modified:
    - "Portfolio.dc.html"
    - "dist/index.html"

key-decisions:
  - "Used pointerdown (not click) for the outside-tap listener so iOS Safari reliably fires it on non-interactive elements"
  - "Guarded outside-click via nav.contains(e.target) DOM check rather than React state, matching the existing setupNavScroll guard pattern and avoiding stale-state races"
  - "Gave Automation its own icon:'gear' key instead of reusing icon:'big', preserving the shared 'big' branch for its 4 other consumers (GIT, DSGVO, ISO 27001, Continually learning)"
  - "Replaced the U+25CC dotted-circle and U+2699 gear emoji with inline SVG using stroke/fill=currentColor, since both codepoints render as uncolourable bitmap glyphs on mobile system fonts"

patterns-established:
  - "New skill icon branches must be inserted between existing sc-if branches and before 'big'/'!sk.icon' fallbacks, matching the net/shield SVG markup convention exactly"

requirements-completed: [QUICK-344]

# Metrics
duration: 5min
completed: 2026-08-03
---

# Quick Task 260803-344: Burger Menu Outside-Click Close + Icon Color Fixes Summary

**Mobile burger menu now closes on outside pointerdown tap; About-me middle bullet and Automation skill tile now render solid-green inline SVG icons instead of faint/uncolourable glyphs.**

## Performance

- **Duration:** ~5 min (commits span 02:19:33 to 02:23:47 local time)
- **Started:** 2026-08-03T00:19:33Z
- **Completed:** 2026-08-03T00:23:47Z
- **Tasks:** 3 automated tasks + 1 checkpoint (deferred, see below)
- **Files modified:** 2 (`Portfolio.dc.html`, `dist/index.html`)

## Accomplishments
- Added `setupOutsideClick()` with a document `pointerdown` listener that closes the mobile menu when a tap lands outside `#main-nav`, reusing the existing `closeMenu()` method and wired into `componentDidMount`/`componentWillUnmount`
- Replaced the `◌` (U+25CC dotted circle) glyph on the middle About-me bullet with an inline concentric-ring SVG that inherits `var(--green)` via `currentColor`
- Replaced the `⚙` (U+2699 gear emoji, uncolourable bitmap on mobile) on the Automation skill tile with a dedicated `icon: 'gear'` sc-if branch rendering an inline SVG cog (hub circle + 8-tooth ring path)
- Mirrored `Portfolio.dc.html` to `dist/index.html`, verified byte-identical via `cmp -s`

## Task Commits

Each task was committed atomically:

1. **Task 1: Close mobile menu on outside tap** - `b653d5e` (feat)
2. **Task 2: Make the About middle icon and the Automation gear render solid green** - `248d0de` (fix)
3. **Task 3: Mirror source to dist and verify byte-identity** - `1df93d2` (chore)

**Plan metadata commit:** pending (orchestrator handles docs commit in Step 8)

## Files Created/Modified
- `Portfolio.dc.html` - Added `setupOutsideClick()` lifecycle method + pointerdown wiring; replaced About middle bullet glyph with SVG; added `icon:'gear'` sc-if branch and switched the Automation SKILLS entry to use it
- `dist/index.html` - Re-mirrored from `Portfolio.dc.html` via literal `cp`, confirmed byte-identical

## Decisions Made
- `pointerdown` over `click` for outside-tap detection (iOS Safari reliability)
- DOM-based `menu.style.display` / `nav.contains()` guards instead of `this.state.menuOpen`, matching the existing `setupNavScroll` pattern
- New gear icon computed as a 16-vertex closed path (8 teeth, outer radius 9.4 / inner radius 6.9, 22.5° steps) rather than 8 separate tooth shapes, to match the plan's "gear-ring path" specification exactly

## Deviations from Plan

None - plan executed exactly as written. All automated verification commands from the plan's `<verify>` blocks and the top-level `<verification>` section passed:

```
grep -c 'setupOutsideClick' Portfolio.dc.html        -> 2
grep -c '_onOutside' Portfolio.dc.html               -> 3
grep -c '◌' Portfolio.dc.html                        -> 0
grep -c '⚙' Portfolio.dc.html                        -> 0
grep -c "sk.icon == 'gear'" Portfolio.dc.html        -> 1
grep -c "icon: 'big'" Portfolio.dc.html              -> 4
cmp -s Portfolio.dc.html dist/index.html             -> IDENTICAL
```

## Issues Encountered

My first draft of the gear SVG path (Task 2) used 8 separate small tooth polygons instead of a single closed 16-vertex ring path as specified by the plan. Caught and corrected before committing by computing the actual 16 vertices (8 teeth, outer radius 9.4 / inner radius 6.9, alternating every 22.5°) and rewriting it as one `<path>`, matching the file's existing space-separated-coordinate convention (no explicit `L` commands, single trailing `Z`). No commit was made with the incorrect version.

## User Setup Required

None - no external service configuration required.

## Deferred: Human Verification Checkpoint

The plan's final task (`checkpoint:human-verify`, gate="blocking") requires manual verification on a real phone (or device-emulated desktop browser) of six checks:

1. Burger closes when tapping outside the open menu (below 675px width)
2. Burger itself still toggles cleanly with no flicker/reopen
3. Menu links (e.g. "Skills") still scroll and close the menu
4. All three About-me bullet icons read as the same green at the same visual weight, with the middle one clearly visible (no faint dotted ring)
5. Automation gear renders green (not grey/black/multicoloured) matching neighbouring AI and Security Officer icons — this specifically requires a real iOS/Android device since the emoji-rendering bug it fixes does not reproduce in desktop browser emulation
6. GIT (⎇), DSGVO (§), ISO 27001 (✪), and "Continually learning" (∞) tiles remain visually unchanged

Per orchestrator instruction, this blocking human-verify checkpoint was not executed interactively. All automated code-level verification (grep counts, byte-identity) passed; the six checks above remain open until a human confirms them on-device.

## Next Phase Readiness
- Source and mirror are in sync and all automated checks pass; no code follow-up needed unless the pending human verification surfaces a defect
- If check 5 (gear color on real device) fails, the fix would be isolated to the new `icon == 'gear'` sc-if branch and would not require touching any other icon branch

---
*Phase: quick-260803-344*
*Completed: 2026-08-03*

## Self-Check: PASSED

- FOUND: Portfolio.dc.html
- FOUND: dist/index.html
- FOUND: b653d5e (Task 1 commit)
- FOUND: 248d0de (Task 2 commit)
- FOUND: 1df93d2 (Task 3 commit)
